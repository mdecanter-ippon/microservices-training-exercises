meta {
  name: List Orders via Gateway
  type: http
  seq: 3
}

get {
  url: {{gateway}}/orders
  body: none
  auth: none
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("Response is an array", function() {
    expect(res.getBody()).to.be.an("array");
  });
}

docs {
  List orders via API Gateway.

  Verifies /orders/* routes to order-service.
}
