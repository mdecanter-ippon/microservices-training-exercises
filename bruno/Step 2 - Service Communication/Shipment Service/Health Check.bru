meta {
  name: Health Check
  type: http
  seq: 1
}

get {
  url: {{shipment_service_url}}/actuator/health
  body: none
  auth: none
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("Service is UP", function() {
    expect(res.getBody().status).to.equal("UP");
  });
}

docs {
  Verify shipment-service is running.
}
