meta {
  name: 6. Check Zipkin Health
  type: http
  seq: 6
}

get {
  url: http://localhost:9411/health
  body: none
  auth: none
}

tests {
  test("Zipkin is running", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

docs {
  Verify Zipkin is running.

  Before running distributed tracing tests:
  docker-compose up -d zipkin
}
